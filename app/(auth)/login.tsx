import { View, Text } from 'react-native';
export default function Login() {
  return <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}><Text>EventOps — Login (Supabase Auth) → Dashboard / Kiosk</Text></View>;
}
